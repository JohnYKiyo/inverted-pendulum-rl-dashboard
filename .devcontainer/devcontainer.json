{
  "name": "Profit Factory",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-20-bullseye",
  "remoteUser": "root",
  "onCreateCommand": "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y locales && grep -q '^ja_JP.UTF-8' /etc/locale.gen || echo 'ja_JP.UTF-8 UTF-8' >> /etc/locale.gen && locale-gen",
  "containerEnv": {
    "LANG": "ja_JP.UTF-8",
    "LC_ALL": "ja_JP.UTF-8"
  },
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "postCreateCommand": "npm install"
}